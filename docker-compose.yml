services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    command: npm run dev --- --host 0.0.0.0

  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"